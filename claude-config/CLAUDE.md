# Global Claude Code memory

<!--
  This is your real, context-rich global memory. It is version-controlled in a
  PRIVATE repo and synced across machines, so there is no need to hold anything
  back here (unlike a public repo). `bootstrap.sh init` copies your existing
  ~/.claude/CLAUDE.md over this seed on the canonical Mac.
-->
